# Backend API base URL (no trailing slash, no /api suffix)
# Local dev: leave empty to use Vite's proxy, or use http://localhost:3000
# Production: set to your Railway backend URL, e.g. https://hr-system-demo.up.railway.app
VITE_API_URL=
